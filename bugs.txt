Server-side bugs:

2. If no transactions for user are present, multiple endpoints fail to account for it
 - fetch endpoint still sends json with no transactions
 - monthly saldo endpoint crashes

3. Adding/Modifying category with a duplicated name results in DB constraint violation. Add schema validation.

4. Conversion to main_amount does not work when creating new transaction manually -
null value in column "main_amount" of relation "transactions" violates not-null constraint
sqlalchemy.exc.IntegrityError: (raised as a result of Query-invoked autoflush; consider using a session.no_autoflush block if this flush is occurring prematurely)

5. Delete category does not work -
urn a valid response. The return type must be a string, dict, tuple, Response instance, or WSGI callable, but it was a list.
e, or WSGI callable, but it was a list.
TypeError: The view function did not return a valid response. The return type must be a string, dict, tuple, Response instance, or WSGI callable, but it was a list.

6. Delete transaction does not work -
value > self.max if self.max_inclusive else value >= self.max
TypeError: can't compare offset-naive and offset-aware datetimes
Exception on /api/transactions/add [POST]
